import * as GameLoader from '../GameLoader'

describe('game loader', () => {
  // beforeEach(() => {
  //   jest.setTimeout(100000);
  // });

  // it('should retrieve games based in current date', async () => {
  //   const games = await GameLoader.loadAll({})

  //   expect(games).not.toBe(null)
  // })
})